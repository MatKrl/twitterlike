<div class="comment-single">
  Commented by: <b><%= link_to comment.user.username, comment.user %></b> |
  <%= comment.user.created_at.strftime("%Y/%m/%d at %R") %>
  <br>
  <%= comment.content %>
</div>
